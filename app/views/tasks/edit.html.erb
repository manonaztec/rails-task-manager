<h1>Tasks#edit</h1>
<p>Find me in app/views/tasks/edit.html.erb</p>

<%= form_for(@task) do |t| %>
  <%= t.label :name %>
  <%= t.text_field :name %>

  <%= t.label :done %>
   <%= t.check_box :done %>

  <%= t.submit %>
<% end %>

<%= link_to "Home page", tasks_path %>
